name: CI

on: [push, pull_request]

jobs:
  ci:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        rust-toolchain: [nightly]
        targets: [x86_64-unknown-linux-gnu, x86_64-unknown-none, riscv64gc-unknown-none-elf, aarch64-unknown-none-softfloat, loongarch64-unknown-none-softfloat, armv7a-none-eabi]
    steps:
    - uses: actions/checkout@v4
    - uses: dtolnay/rust-toolchain@nightly
      with:
        toolchain: ${{ matrix.rust-toolchain }}
        components: rust-src, clippy, rustfmt
        targets: ${{ matrix.targets }}
    - name: Check rust version
      run: rustc --version --verbose
    - name: Check code format
      run: cargo fmt --all -- --check
    - name: Clippy (zero-vma)
      run: cargo clippy --target ${{ matrix.targets }} --features "preempt,arm-el2"
    - name: Build (zero-vma)
      run: cargo build --target ${{ matrix.targets }} --features "preempt,arm-el2"
    - name: Clippy (non-zero-vma)
      run: cargo clippy --target ${{ matrix.targets }} --features "preempt,arm-el2,non-zero-vma"
    - name: Build (non-zero-vma)
      run: cargo build --target ${{ matrix.targets }} --features "preempt,arm-el2,non-zero-vma"
    - name: Unit test
      if: ${{ matrix.targets == 'x86_64-unknown-linux-gnu' }}
      run: |
        cargo test --target ${{ matrix.targets }} --features "sp-naive,non-zero-vma" -- --nocapture
        cargo test --target ${{ matrix.targets }} --features "non-zero-vma" -- --nocapture

  doc:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: write
    env:
      default-branch: ${{ format('refs/heads/{0}', github.event.repository.default_branch) }}
      RUSTDOCFLAGS: -Zunstable-options --enable-index-page -D rustdoc::broken_intra_doc_links -D missing-docs
    steps:
    - uses: actions/checkout@v4
    - uses: dtolnay/rust-toolchain@nightly
    - name: Build docs
      continue-on-error: ${{ github.ref != env.default-branch && github.event_name != 'pull_request' }}
      run: cargo doc --no-deps
    - name: Deploy to Github Pages
      if: ${{ github.ref == env.default-branch }}
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        single-commit: true
        branch: gh-pages
        folder: target/doc
